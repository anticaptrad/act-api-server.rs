console.error('`npm run create` is intentionally disabled because it is ambiguous.');
console.error('Use `npm run bind` for the existing Anticaptrad project or `npm run create:new` for a separate project.');
process.exit(2);
