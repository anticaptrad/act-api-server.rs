name: Sync graceful-shutdown generated files

on:
  push:
    branches:
      - agent/graceful-shutdown-contract
    paths:
      - Cargo.toml
      - Cargo.lock
      - src/server.rs
      - src/server/shutdown.rs
      - .github/workflows/sync-shutdown-lockfile.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-graceful-shutdown-generated-files
  cancel-in-progress: true

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out rollout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Migrate call sites to axum-server 0.7.3
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          server = Path("src/server.rs")
          text = server.read_text()
          old = "axum_server::from_tcp(listener)?\n        .handle(server_control.clone())"
          new = "axum_server::from_tcp(listener)\n        .handle(server_control.clone())"
          if old in text:
              server.write_text(text.replace(old, new, 1))
          elif new not in text:
              raise SystemExit("src/server.rs does not match either supported axum-server call form")

          shutdown = Path("src/server/shutdown.rs")
          text = shutdown.read_text()
          old = "axum_server::from_tcp(listener)\n            .expect(\"create test server\")\n            .handle(handle.clone())"
          new = "axum_server::from_tcp(listener)\n            .handle(handle.clone())"
          if old in text:
              shutdown.write_text(text.replace(old, new, 1))
          elif new not in text:
              raise SystemExit("shutdown test does not match either supported axum-server call form")
          PY

      - name: Apply canonical Rust formatting
        run: cargo fmt --all

      - name: Resolve dependency graph and compile every test target
        run: cargo test --no-run --all-targets --all-features

      - name: Commit only generated shutdown artifacts
        shell: bash
        run: |
          set -euo pipefail

          changed="$(git diff --name-only)"
          if [[ -z "$changed" ]]; then
            echo 'Generated shutdown artifacts are already current.'
            exit 0
          fi

          unexpected="$(printf '%s\n' "$changed" | grep -Ev '^(Cargo\.lock|src/server\.rs|src/server/shutdown\.rs)$' || true)"
          if [[ -n "$unexpected" ]]; then
            echo 'Refusing to commit unexpected generated changes:' >&2
            printf '%s\n' "$unexpected" >&2
            exit 1
          fi

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Cargo.lock src/server.rs src/server/shutdown.rs
          git commit -m 'chore: sync shutdown generated artifacts'
          git push origin "HEAD:${GITHUB_REF_NAME}"
